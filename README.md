#Let's install npm, by running this command from Terminal,
#Let's run the MongoDb.
#Let's run the server.js.
#Now, visit http://localhost:3600 to try the demo.

# Note
This demo use mongodb database.










